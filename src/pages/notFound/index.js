import Erro from "../../img/Erro.png";

const NotFound = () => {
  return (
    <div>
      <img style={{ width: "100vw", height: "100vh" }} src={Erro} />
    </div>
  );
};

export default NotFound;
